import { BASELINE } from "~/settings/typography";
import COLOURS from "~/settings/colours";
import { rem } from "~/utils";

const HERO_IMAGE_SHADOW = `0 ${rem(BASELINE * 1)} ${rem(BASELINE * 2)} ${
  COLOURS.SHADOW
}`;

export { HERO_IMAGE_SHADOW };
